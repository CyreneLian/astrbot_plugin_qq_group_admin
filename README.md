# 🌸 QQ 群大模型管理工具插件 (`astrbot_plugin_qq_group_admin`)

一款专门为 **AstrBot** 注册原生大模型工具（LLM Tool）的轻量级 QQ 群管理与互动插件。无需记忆任何死板的前缀或正则表达式命令，让大模型直接理解群管理或群友的自然语言诉求（如“把小明禁言5分钟”、“把那个打广告的踢出群聊并拉黑”、“撤回某某最近的几条消息”、“把小明刚发的消息设为群精华”、“查一下张三最近什么时候发言”、“清理超过60天未发言的潜水成员”、“戳一下某某”等），并自动安全地调用底层 OneBot API 执行相关群管与互动操作。

---

## ✨ 核心特性

1. **大模型原生 16 大 Tool 组合**：
   - `ban_group_member`：对指定群成员执行禁言或解除禁言。
   - `kick_group_member`：将指定群成员移除群聊（踢人），支持是否同时拒绝后续加群申请（拉黑/黑名单）。
   - `delete_group_message`：撤回单条或多条指定消息，支持指定单个或多个 ID（用逗号/空格分隔）进行批量撤回，亦支持回复/引用消息时自动识别并撤回。
   - `set_group_essence_message`：设置或取消群精华消息，支持回复/引用消息时自动识别并设为精华/移除精华。
   - `get_group_msg_history`：获取群聊历史消息记录（单次最多可调取 100 条历史记录），协助上下文寻找发言 ID。
   - `set_group_whole_ban`：开启或解除全员禁言（全体禁言）。
   - `set_group_card`：设置或修改指定群成员的群名片/群昵称（支持重置清空名片）。
   - `set_group_special_title`：为指定群成员颁发专属头衔（支持指定有效天数或永久有效，需 Bot 为群主）。
   - `set_group_admin`：设置或取消某位成员的群管理员身份（需 Bot 为群主）。
   - `set_group_name`：修改当前 QQ 群聊的名称。
   - `send_group_notice`：在群内发布全新的官方群公告/群通知。
   - `get_group_info`：获取当前群聊的详细信息（群名称、群主 QQ、当前成员数与容量上限等）。
   - `get_group_member_list`：获取全员列表，支持按关键词（昵称/名片/QQ号）精准搜索与筛选群成员（含最后发言时间标注）。
   - `get_group_member_info`：查询指定群成员的详细个人资料（精确查看最近发言时间、入群时间、群身份、头衔、禁言状态等）。
   - `kick_inactive_members`：查找或清理长时间未发言的潜水成员（自动跳过群主和管理员，支持指定天数与二段安全预览确认）。
   - `group_poke`：对指定群成员执行调皮的“戳一戳”（拍一拍/戳一下）动作。
2. **灵活的权限控制与普通群友授权**：
   - `allow_bot_admin`：是否允许 Bot 配置文件中指定的超级管理员使用（默认 `true`）。
   - `allow_group_owner`：是否允许目标群的群主使用（默认 `true`）。
   - `allow_group_admin`：是否允许目标群的管理员使用（默认 `true`）。
   - `member_allowed_tools`：允许普通群友调用的功能工具列表（可通过配置面板勾选纯中文工具名如“清理潜水成员”、“查询成员信息”，授权给普通群成员使用）。
3. **安全严密防护**：工具被大模型触发时，会在函数底层二次强校验发送者的实际身份与勾选授权列表，防止普通群成员越权使用敏感群管命令。
4. **友好的异常反馈**：若 Bot 自身在群内缺乏管理员/群主权限或 API 调用失败，大模型能获得清晰明确的回执说明。

---

## ⚙️ 配置说明

在 AstrBot 管理面板的插件配置页面中，支持配置以下参数：

| 配置项 (Key) | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| `allow_bot_admin` | bool | `true` | 是否允许 **Bot 超级管理员**（`admins_id` 列表中的账号）使用管理工具 |
| `allow_group_owner` | bool | `true` | 是否允许**目标群群主**使用管理工具 |
| `allow_group_admin` | bool | `true` | 是否允许**目标群管理员**使用管理工具 |
| `member_allowed_tools` | list | `["读取历史消息", "获取群信息", "获取全员列表", "查询成员信息", "戳一戳"]` | **允许普通群友使用的功能工具**（勾选的功能工具将允许普通群友直接指挥 Bot 调用使用） |

---

## 🚀 使用方法与指令范例

插件无需注册任何死板的前缀命令。只要当前发送者满足管理员权限要求，或调用的特定功能已被勾选授权给普通群友使用，在 QQ 群聊中直接对 Bot 提出自然语言要求即可：

### 1. 禁言与解禁（`ban_group_member`）
- **常规禁言**：“把小明禁言 5 分钟” / “给小明套餐半小时”
- **解除禁言**：“把小明放出来” / “解除小明的禁言”
- **带原因禁言**：“把小明禁言 10 分钟，原因：刷屏打广告”

### 2. 踢出群聊（`kick_group_member`）
- **常规踢人**：“把张三踢出群聊” / “把张三移出本群”
- **踢人并拉黑**：“把那个打广告的踢出去，并且拒绝他再申请加群” / “把张三移除群聊并拉黑”
- **带原因踢人**：“把张三踢了，原因：违规言论”

### 3. 撤回消息（`delete_group_message`）
- **引用撤回**：（回复/引用某条违规消息）“把这条消息撤回” / “撤回他刚才发的内容”
- **指定 ID 撤回**：“撤回张三刚才发的那条消息”

### 4. 群精华消息（`set_group_essence_message`）
- **引用设为精华**：（回复/引用某条优质消息）“把这条设为精华” / “设为群精华消息”
- **引用取消精华**：（回复/引用某条已设为精华的消息）“取消精华” / “把这条消息移除群精华”
- **指定 ID 设精华**：“把张三刚才发的那条设为精华”

### 5. 读取历史消息（`get_group_msg_history`）
- **查历史**：“看看群里最近发了什么” / “获取最近 50 条群历史消息”

### 6. 全体禁言（`set_group_whole_ban`）
- **开启全体禁言**：“开启全员禁言” / “开启全体禁言”
- **解除全体禁言**：“解除全员禁言” / “恢复全员发言”

### 7. 修改群名片（`set_group_card`）
- **修改名片**：“把小明的群名片改成小笨蛋”
- **重置名片**：“清空小明的群名片”

### 8. 专属头衔（`set_group_special_title`）
- **设置头衔**：“给张三颁发一个‘群宠’专属头衔”

### 9. 管理员设置（`set_group_admin`）
- **设置管理员**：“把小明设为群管理员”
- **取消管理员**：“取消小明的管理员身份”

### 10. 修改群名称（`set_group_name`）
- **修改群名**：“把群名称改成星穹列车聊天群”

### 11. 发布群公告（`send_group_notice`）
- **发公告**：“发个群公告：今晚 8 点开会，大家准时参加”

### 12. 获取群信息（`get_group_info`）
- **查群信息**：“看看咱们这个群的信息”

### 13. 获取成员列表与搜索（`get_group_member_list`）
- **查看成员**：“查看群成员列表”
- **按关键词搜索**：“搜一下群里有没有叫小明的” / “看看群里有哪些管理员”

### 14. 查询单人成员资料与最后发言时间（`get_group_member_info`）
- **查成员发言时间**：“查一下张三最近什么时候发言” / “查询张三的详细群资料”

### 15. 查找与清理潜水成员（`kick_inactive_members`）
- **安全扫描预览**：“查找群里超过 60 天没发言的人” / “看看有哪些人两个月没发言”
- **确认一键清理**：“确认清理超过 60 天未发言的潜水成员”

### 16. 戳一戳（`group_poke`）
- **戳一下**：“戳一下小明” / “拍一拍小明”

---

## ❓ 常见问题 (FAQ)

- **为什么在插件里勾选开启了“群主”、“群管理员”权限，但他们依然无法调用插件？**
  - 这通常是因为在 **AstrBot 全局配置** 中开启了 **“需要管理员权限才能使用电脑能力 / 工具”** 这个底层安全开关。
  - 该全局开关是 AstrBot 框架最高层级的安全门，一旦开启，框架会在接收到请求的入口处直接拦截所有非 Bot 超级管理员（`admins_id`）的工具调用，请求根本无法触达插件内部。
  - **解决办法**：若希望群主、群管理员或被授权的普通群友能够正常调用插件工具，请在 AstrBot 全局配置中关闭该开关，由本插件自带的权限控制进行精细化拦截与保护。

- **为什么提示“Bot 自身在群中缺乏管理员权限”？**
  - 实际在 QQ 服务端执行禁言、踢人、撤回、设置精华、修改群名片等动作的是 **Bot 账号自身**。因此，请确保 Bot 账号在当前 QQ 群中拥有**管理员**或**群主**身份。

- **为什么我是 Bot 管理员但在群里是普通成员也能成功使用？**
  - 只要配置中的 `allow_bot_admin` 开启，且你的 QQ 号登记在 AstrBot `全局配置` -> `admins_id` 列表中，插件内部就会识别并授权。只要 Bot 账号自身在群里是管理员，即可成功执行操作！

- **如何让普通群友使用某个特定的功能？**
  - 只需要在插件配置页面的 `member_allowed_tools` 多选列表中，勾选对应的纯中文功能名称（例如勾选 `戳一戳`、`查询成员信息` 或 `获取群信息`）并保存，普通群成员发自然语言指令时即可直接调用！

---

## 作者

- 往昔的涟漪

## 联系方式

- QQ: 1158026885

## 如果这个插件对你有帮助，欢迎给个 ⭐ Star！
