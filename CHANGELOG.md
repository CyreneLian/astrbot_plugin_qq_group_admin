# 📊 astrbot_plugin_qq_group_admin CHANGELOG

## 🌸 v2.2.0 (2026-08-09)

> **版本定位**：查询能力大幅增强、底层工具描述严密化与新增@工具等的重磅升级大版本。

### 🌟 核心功能增强
- **新增 @ 成员与 @全体成员 操作授权工具 (`at_all_members`)**：
  - 支持普通 @成员 的智能解析与零宽防粘连保护和通过专用工具发起 @全体成员 的高亮唤醒与权限预检，结合底层双重权限校验（发送者管理员权限 + Bot 账号自身群管理员/群主身份预检），并与消息拦截器完成原生高亮 At 组件合流发送。
- **全管理工具 Bot 自身身份强效校验**：
  - 在通用权限检测中增加 Bot 账号自身的角色预检（支持管理员与群主双门槛），防止在非标准OneBot接口的情况下，Bot 无管理员权限时出现“静默假成功”等问题。
- **全员列表查询与多维排序升级 (`get_group_member_list`)**：
  - **新增多重与升降序排序**：支持按`进群时间`、`最近发言时间`或`群等级`进行升序/降序动态排序，方便快速查找“最久未发言的潜水成员”、“最低/最高群等级成员”或“最新/最早进群的成员”。
  - **新增身份特征检索**：在搜索逻辑中加入 `role_cn` 和 `role` 字段匹配，可直接通过关键词“管理员”、“群主”等自然语言精准检索管理层名单。
  - **时序与群等级档案补全**：在全员列表每行输出中，补全直观的 `LV.{level}` 群等级与“入群时间”格式化展示；并在单人查询（`get_group_member_info`）中同步支持群等级检索。
- **长时间未发言潜水成员清理增强 (`kick_inactive_members`)**：
  - **支持结合【群等级】双重门槛筛选**：可按指定天数与群等级上限（如低于 LV.10）进行精准潜水清理；若预检模式下未指定等级门槛，大模型将提示用户可补充等级要求以保护活跃老群成员防误踢。
- **新增戳一戳被动响应与配置控制 (`on_poke`)**：
  - **被动互动响应**：支持当有人在群聊/私聊中戳一戳 Bot 时，触发 LLM 契合角色性格的自然回应；支持在插件配置中开启/关闭此响应。
- **新增群聊黑名单防护功能与配置拦截 (`blacklisted_groups`)**：
  - **全功能黑名单屏蔽**：支持配置黑名单群聊列表，在黑名单列表中的群聊将完全屏蔽与禁用本插件的所有功能（含 17 大 LLM 工具调用、@ 成员指令与被动戳一戳响应）。
- **群公告工具全能化升级 (`send_group_notice`)**：
  - **三合一能力整合**：将 NapCat 扩展 API `_get_group_notice`（读取群公告）、`_del_group_notice`（删除群公告）整合进原有发布公告工具，支持发布/读取/删除三种操作模式，通过 action 参数自动分流。
- **批量撤回安全机制强化 (`delete_group_message`)**：
  - **单次撤回硬性上限**：在代码底层加入安全截断逻辑，限制单次批量撤回最多 10 条消息（`id_list[:10]`），防止因传参过多触发 QQ 协议端风控拦截。

### 📖 底层工具描述优化
- **底层工具描述严密化**：对全部 17 个管理与互动工具的 Docstring 进行排查与严密化精修，使大模型能更好理解工具功能和使用场景。

## 🌸 v2.1.0 (2026-08-05)

> **版本定位**：全套 QQ 群大模型管理与互动工具重磅升级大版本。

### 🌟 全新 16 大 Tool 大模型管理与互动工具
- **全套管理工具链**：集成禁言、踢人/拉黑、撤回消息、设群精华、读取历史消息、全体禁言、修改群名片、专属头衔、设置/取消管理员、修改群名称、发布群公告、获取群信息、全员列表与搜索、查询成员资料与发言时间、清理潜水成员、戳一戳等全套工具。
- **长时间未发言潜水成员清理 (`kick_inactive_members`)**：支持按指定天数（如 60 天）扫描潜水人员，具备二段式安全预览确认机制，并自动跳过群主和管理员防误踢。
- **支持单次批量撤回消息 (`delete_group_message`)**：`message_id` 参数升级支持接收逗号、分号或空格分隔的多个消息 ID，大模型单次工具调用即可完成多条违规消息的并发批量撤回，大幅提升响应速度并节省 Token。
- **单人成员资料与发言时间查询 (`get_group_member_info`)**：支持精确查询某位群成员的最近发言时间、入群时间、群身份、头衔及禁言解禁状态。
- **历史消息列表优化**：全员搜索与列表结果中追加格式化显示成员的最近发言时间 (`MM-DD HH:MM`)。

### ⚙️ 权限控制与配置升级
- **纯中文多选框授权机制 (`member_allowed_tools`)**：在插件配置面板中加入纯中文功能多选框，支持动态勾选并将特定工具（如“戳一戳”、“获取群信息”、“查询成员信息”）授权给普通群友使用。
- **后台严格权限校验**：所有敏感管理工具在底层实施强校验，确保未授权的普通群成员无法越权命令 Bot 执行敏感群管指令。

### 📖 文档与体验优化
- **README 使用文档全面升级**：更新全套 16 大 Tool 核心特性说明、配置表格及 FAQ 常见问题说明。
- **简介与说明精修**：重新梳理精简了插件简介与配置项提示文案。
